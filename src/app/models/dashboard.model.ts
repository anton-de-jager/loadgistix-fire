import { status } from "./status.model";
import { user } from "./user.model";

export class dashboard {
    category?: string;
    id?: string;
    description?: string;
    date?: Date;
    value?: number;
}